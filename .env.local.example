DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=saas_app

# Optional: PlantUML diagram source is rendered by posting it to this server.
# Defaults to the public plantuml.com service if unset. For confidential
# architecture data, point this at a self-hosted PlantUML server instead,
# e.g. http://plantuml.internal:8080
# PLANTUML_SERVER_URL=https://www.plantuml.com/plantuml
